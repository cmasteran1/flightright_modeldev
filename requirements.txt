# Flight delay model — Python dependencies
# Tested with Python 3.10+
# Install with: pip install -r requirements.txt

# ── Core data / ML ────────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.0.0
pyarrow>=14.0.0          # Parquet read/write backend
scikit-learn>=1.5.0
catboost>=1.2.5
joblib>=1.3.0

# ── Weather API ───────────────────────────────────────────────────────────────
requests>=2.31.0

# ── Experiment tracking ───────────────────────────────────────────────────────
# MLflow is used by the trainers and hyperparam_optimize.py to log params,
# metrics, and artifacts into ../flightrightdata/mlruns/. Browse runs with
#   mlflow ui --backend-store-uri file://../flightrightdata/mlruns
mlflow>=2.10.0

# ── Hyperparameter optimization ───────────────────────────────────────────────
optuna>=3.5.0

# ── Timezone handling ─────────────────────────────────────────────────────────
# zoneinfo is built-in Python 3.9+; backport for older Python:
# tzdata>=2024.1          # Required on Windows (zoneinfo backend)

# ── Optional: visualisation (reliability plots) ───────────────────────────────
# matplotlib>=3.8.0

# ── Notes ─────────────────────────────────────────────────────────────────────
# CatBoost ships its own binary; no CUDA driver required for CPU training.
# GPU training (faster): install catboost with GPU support and set
#   catboost param: task_type="GPU"
# On Apple Silicon (M1/M2/M3) catboost>=1.2 runs natively via ARM build.
